<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Simple Web App' %></title>
    <link href="/css/styles.css" rel="stylesheet">
    <script src="https://unpkg.com/htmx.org@1.9.8"></script>
</head>
<body class="bg-gray-50 min-h-screen">
    <nav class="bg-blue-600 text-white shadow-lg">
        <div class="container mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <h1 class="text-xl font-bold">
                    <a href="/" class="hover:text-blue-200">Simple Web App</a>
                </h1>
                <div class="space-x-4">
                    <% if (user) { %>
                        <span class="text-blue-200">Hello, <%= user.username %>!</span>
                        <a href="/dashboard" class="hover:text-blue-200">Dashboard</a>
                        <form method="POST" action="/logout" class="inline">
                            <button type="submit" class="hover:text-blue-200 bg-transparent border-none cursor-pointer">Logout</button>
                        </form>
                    <% } else { %>
                        <a href="/login" class="hover:text-blue-200">Login</a>
                        <a href="/register" class="hover:text-blue-200">Register</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-4 py-8">
        <%- body %>
    </main>

    <footer class="bg-gray-800 text-white mt-12">
        <div class="container mx-auto px-4 py-6 text-center">
            <p>&copy; 2025 Simple Web App. Built with Node.js, Express, and Tailwind CSS.</p>
        </div>
    </footer>
</body>
</html>