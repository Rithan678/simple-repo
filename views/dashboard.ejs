<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Simple Web App</title>
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body class="bg-gray-50 min-h-screen">
    <nav class="bg-blue-600 text-white shadow-lg">
        <div class="container mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <h1 class="text-xl font-bold">
                    <a href="/" class="hover:text-blue-200">Simple We</a>
                </h1>
                <div class="space-x-4">
                    <span class="text-blue-200">Hello, <%= user.username %>!</span>
                    <form method="POST" action="/logout" class="inline">
                        <button type="submit" class="hover:text-blue-200 bg-transparent border-none cursor-pointer">Logout</button>
                    </form>
                </div>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-4 py-8">
        <div class="flex justify-between items-center mb-8">
            <h2 class="text-3xl font-bold text-gray-800">Your Dashboard</h2>
            <a href="/posts/new" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                New Post
            </a>
        </div>

        <% if (posts.length === 0) { %>
            <div class="text-center py-12">
                <p class="text-gray-600 text-lg mb-4">You haven't created any posts yet.</p>
                <a href="/posts/new" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                    Create Your First Post
                </a>
            </div>
        <% } else { %>
            <div class="grid gap-6">
                <% posts.forEach(function(post) { %>
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <div class="flex justify-between items-start mb-4">
                            <h3 class="text-xl font-semibold text-gray-800"><%= post.title %></h3>
                            <div class="space-x-2">
                                <a href="/posts/<%= post.id %>/edit" class="text-blue-600 hover:underline">Edit</a>
                                <form method="POST" action="/posts/<%= post.id %>/delete" class="inline">
                                    <button type="submit" class="text-red-600 hover:underline bg-transparent border-none cursor-pointer"
                                            onclick="return confirm('Are you sure you want to delete this post?')">Delete</button>
                                </form>
                            </div>
                        </div>
                        <p class="text-gray-700 mb-4"><%= post.content %></p>
                        <p class="text-sm text-gray-500">Created: <%= new Date(post.created_at).toLocaleDateString() %></p>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </main>
</body>
</html>